node_modules/
.next/
.playwright-cli/
.env
.env.local
recordings/
output/
public/
scripts/*.json

# keep browse-plan.json — it's the single config file you edit, and the composition imports it
!scripts/browse-plan.json
